/** Base URL of the CDN hosting default ROM images (and the Einstein Xtal DOS
 *  boot disk). Neutral constant so machine descriptors and the shared
 *  rom-manager can build URLs without importing each other. */
export const ROM_BASE = 'https://zx84files.bitsparse.com/roms/';
